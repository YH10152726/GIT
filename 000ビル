        ' 次のブロックの開始条件（AGに品番があり、Yに「同時」がない）に合致したら、その手前が最終行
        If (regExAG.test(agValue) And Not regExY.test(yValue)) Or UCase(agValue) = "END" Then
            FindEndOfBlock = i - 1
            Exit Function
        End If
    Next i
End Function
